import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export default function appsetings() {
    return (
        <div>
            <h1>Application Settings</h1>
        </div>
    );
}